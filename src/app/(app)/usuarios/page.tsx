import { copy } from "@/lib/copy/es-AR";
import { PlaceholderPage } from "../PlaceholderPage";

export default function UsuariosPage() {
  return (
    <PlaceholderPage kicker={copy.usuarios.kicker} title={copy.usuarios.title} />
  );
}
