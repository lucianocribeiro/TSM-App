import { copy } from "@/lib/copy/es-AR";
import { PlaceholderPage } from "../PlaceholderPage";

export default function LegajosPage() {
  return (
    <PlaceholderPage kicker={copy.legajos.kicker} title={copy.legajos.title} />
  );
}
