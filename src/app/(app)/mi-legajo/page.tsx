import { copy } from "@/lib/copy/es-AR";
import { PlaceholderPage } from "../PlaceholderPage";

export default function MiLegajoPage() {
  return (
    <PlaceholderPage kicker={copy.miLegajo.kicker} title={copy.miLegajo.title} />
  );
}
